//calculate the area of square 
#include <stdio.h>

int main() {
   int side ,area ;
   printf("enter the side :");
   scanf("%d", &side);
   area = side * side ;
   printf("%d",area);
    return 0;
}