#include<stdio.h>
int main()
{
   int a,b ,sum ;
   printf("enter the number a :");
   scanf("%d", &a);
   printf("enter the number b:");
   scanf("%d", &b);
   sum = a + b ;
   printf("sum %d",sum );
    return 0;
}
